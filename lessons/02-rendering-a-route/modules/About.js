import React from 'react';

export default React.createClass({
  render() {
    return (
    	<div>
    		<h2>About React Router!</h2>
    		<p>Use Router, Route to handler route of your app</p>
  		</div>
    );
  }
})
